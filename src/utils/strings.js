const TOKEN = 'token';
const TOKENS = 'tokens';
const ADMIN = 'admin';
const USER = 'user';
const USERS = 'users';
const EMAIL = 'email';

module.exports = {
  TOKEN,
  TOKENS,
  ADMIN,
  USER,
  USERS,
  EMAIL,
};
