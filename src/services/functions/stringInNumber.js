const { PARSE_INT_RADIX } = require('../../utils/magicNumbers');

module.exports = (string) => parseInt(string, PARSE_INT_RADIX);

/*

Função que efetua a troca de elementos tipo numero para tipo string

*/
